name: Build

on:
  push:
    branches:
      - 'main'

jobs:
  master_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Echo 123
        run: echo "123"
