name: Dependent Pipeline

on:
  push:
    branches-ignore:
      - main

  workflow_run:
    workflows: ["Main Pipeline"]
    branches:
      - main
    types:
      - completed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build project
        run: echo 123
